// Street

var streets_default = WaveSurfer.create({
    container: '#streets_default',
    waveColor: '#2a2a2a',
    progressColor: '#ff530d',
    barWidth: 3
});
streets_default.load('../assets/sounds/street_default.ogg');

var streets_minus = WaveSurfer.create({
    container: '#streets_minus',
    waveColor: '#2a2a2a',
    progressColor: '#00c05b',
    barWidth: 3
});
streets_minus.load('../assets/sounds/street_minus-32db.ogg');

var streets_minus_plus = WaveSurfer.create({
    container: '#streets_minus_plus',
    waveColor: '#2a2a2a',
    progressColor: '#00c05b',
    barWidth: 3
});
streets_minus_plus.load('../assets/sounds/street_minus-36db.ogg');

// Construction

var construction_default = WaveSurfer.create({
    container: '#construction_default',
    waveColor: '#2a2a2a',
    progressColor: '#ff530d',
    barWidth: 3
});
construction_default.load('../assets/sounds/construction_default.ogg');

var construction_minus = WaveSurfer.create({
    container: '#construction_minus',
    waveColor: '#2a2a2a',
    progressColor: '#00c05b',
    barWidth: 3
});
construction_minus.load('../assets/sounds/construction_minus-32db.ogg');

var construction_minus_plus = WaveSurfer.create({
    container: '#construction_minus_plus',
    waveColor: '#2a2a2a',
    progressColor: '#00c05b',
    barWidth: 3
});
construction_minus_plus.load('../assets/sounds/construction_minus-36db.ogg');

// Highway

var highway_default = WaveSurfer.create({
    container: '#highway_default',
    waveColor: '#2a2a2a',
    progressColor: '#ff530d',
    barWidth: 3
});
highway_default.load('../assets/sounds/highway_default.ogg');

var highway_minus = WaveSurfer.create({
    container: '#highway_minus',
    waveColor: '#2a2a2a',
    progressColor: '#00c05b',
    barWidth: 3
});
highway_minus.load('../assets/sounds/highway_minus-32db.ogg');

var highway_minus_plus = WaveSurfer.create({
    container: '#highway_minus_plus',
    waveColor: '#2a2a2a',
    progressColor: '#00c05b',
    barWidth: 3
});
highway_minus_plus.load('../assets/sounds/highway_minus-36db.ogg');
